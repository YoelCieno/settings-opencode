# Skills-store repo (pencil)

Idea: extract `skills-store/` into a separate repo (e.g. `skill-registry`) for
cross-project sharing via git submodule or symlink into `.agents/skills/`
per-project.

Deferred — no action yet.
