# Design

No implementation. Artifacts-only change: proposal + one spec delta + tasks.
Validation via `openspec validate phase3-smoke`; archival via `openspec archive -y phase3-smoke`.
No code, config, or migration impact.
